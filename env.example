# Server Configuration
PORT=3000
NODE_ENV=development

# AWS S3 Configuration
# Get these from AWS IAM after creating a user with S3 permissions
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-aws-access-key-id-here
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key-here
S3_BUCKET_NAME=fraud-detection-service-data

# Instructions:
# 1. Copy this file to .env: cp env.example .env (or copy-paste contents)
# 2. Replace the placeholder values with your actual credentials
# 3. Never commit .env to git (it's already in .gitignore)
# 4. For production, use AWS Secrets Manager or environment variables set by your hosting provider



